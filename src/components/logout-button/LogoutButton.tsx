import { Button } from "@mui/material";
const LogoutButton = () => {
    return (
        <Button variant="contained" onClick={() => {}}>
            Log Out
        </Button>
    );
};
export default LogoutButton;
